# 教师登录密码（首次启动 seed 时会写入数据库，之后改密码请用 /api/auth/change-password）
TEACHER_PASSWORD=classpet123

# 服务端口
PORT=3000

# 数据库文件路径
DB_PATH=./data/classpet.db

# 上传目录
UPLOADS_PATH=./uploads

# 前端静态资源目录（指向项目根的 index.html / css / js）
PUBLIC_PATH=../
